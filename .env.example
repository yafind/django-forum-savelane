# Example .env for developers. Copy to .env and fill real values.

# SECRET_KEY: keep secret; use a different value in production
SECRET_KEY=change-me-to-a-secure-value

# DEBUG: True for local development, False for production
DEBUG=True

# Database
DB_NAME=forum_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Comma-separated ALLOWED_HOSTS
ALLOWED_HOSTS=localhost,127.0.0.1


